#####################################################

To execute the program, follow these steps:

1. Run the 'ml_val.py' file first to compute the classifiers' hyperparamters. Type the following command in the terminal:
   python3 ml_val.py 

2. After the completion of the above script, run the 'ml_test.py' file to compute the results. Type  the following command in the terminal:
   python3 ml_test.py


#####################################################